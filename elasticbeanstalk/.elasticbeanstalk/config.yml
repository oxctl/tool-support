branch-defaults:
  default:
    environment: ${AWS_EB_ENVIRONMENT}
environment-defaults:
  ${AWS_EB_ENVIRONMENT}:
    branch: null
    repository: null
global:
  application_name: ${AWS_EB_APPLICATION}
  default_ec2_keyname: ctl-sys-apps
  default_platform: docker
  default_region: ${LTIAUTHSERVERCONFIG_POINTER_REGION}
  include_git_submodules: true
  instance_profile: null
  platform_name: null
  platform_version: null
  profile: null
  sc: null
  workspace_type: Application
